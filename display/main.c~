#include <stdio.h>
#include <stdlib.h>

#define MAX_Y 60
#define MAX_X 234

int main( void  )
{
	for( int i = 0; i < MAX_Y; i++ )
	{
		for( int j = 0; j < MAX_X; j++ )
		{
			printf( "0" );
		}
		putchar( '\n' );
	}
	return 0;
}
